export const MODULE_ID = 'pp-dice';

export const SETTINGS = {
  ENABLED: 'enabled',
} as const;

export const FLAGS = {
  PHYSICAL_ROLL: 'isPhysicalRoll',
  ORIGINAL_RESULTS: 'originalResults',
} as const;
